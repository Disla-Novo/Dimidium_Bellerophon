# Dimidium web server configuration.
# This file lives next to SyntaxN.jar (see run.bat/run.sh). If it's missing,
# or server.port is empty/invalid, the server falls back to port 4567.
server.port=4567
